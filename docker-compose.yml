version: "3.8"
services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: booklovers_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: nagahashi98
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
